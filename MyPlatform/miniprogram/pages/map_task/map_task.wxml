<map id="map" longitude="{{longitude}}"
latitude="{{latitude}}" scale="12" 
    controls="{{controls}}" 
    markers="{{markers}}" 
     bindmarkertap="markertap" 
    style="width: 100%; height: 90vh;"></map>

<button class="choose-my-location" bindtap="chooseLocation">选择位置</button>